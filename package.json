{
  "name": "dashware-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd proyectoFinal && npm ci && npx --yes @angular/cli@20 build --configuration production"
  }
}
