{
  "name": "dashware-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd proyectoFinal && npm install && ./node_modules/.bin/ng build --configuration production"
  }
}
