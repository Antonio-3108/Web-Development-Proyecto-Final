{
  "name": "dashware-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:frontend": "cd proyectoFinal && npm install",
    "install:backend": "cd backend && npm install",
    "build": "cd proyectoFinal && npm install && npm run build",
    "vercel-build": "cd proyectoFinal && npm install && npm run build"
  },
  "workspaces": [
    "proyectoFinal",
    "backend"
  ]
}
